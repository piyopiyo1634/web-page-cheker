name: test-minimal

on:
  workflow_dispatch:

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      - name: Fetch test page
        run: |
          mkdir -p snapshots
          curl -s "http://example.com" > snapshots/current.html
          echo "fetched ok"
